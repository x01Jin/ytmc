# Optional local overrides. Copy to `.env` to use.
# Development default: PORT=3000, HOST=127.0.0.1
# Production default (`npm run start`): random free port, HOST=127.0.0.1
# Set an explicit port if you need a stable URL (e.g. desktop wrapper config).
PORT=3000
HOST=127.0.0.1
